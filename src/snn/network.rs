struct Network {
    neurons: Vec<Neuron>,
}

impl Network {
    // new should create a new Network with the given number of neurons.
    // If it can fail, use build instead
    fn new(???) -> Network {
        todo!()
    }
}