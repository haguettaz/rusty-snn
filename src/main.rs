use rusty_snn::spike_train::spike_train::{PeriodicSpikeTrain, SpikeTrain};

// use rand::distributions::WeightedIndex;
use rand::prelude::*; // for thread_rng()

// use rusty_snn::spike_train::utils::prob_num_spikes;


fn main() {
    // let mut rng = StdRng::seed_from_u64(42);

    // let ref_spike_train = PeriodicSpikeTrain::random(100.0, 1.0, 100, &mut rng).unwrap();
    // let sim_spike_train = SpikeTrain::build(
    //     vec![vec![2.0, 3.0, 4.0], vec![1.2, 3.2]],
    //     1.0,
    //     5.0,
    // )

}
